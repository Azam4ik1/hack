{
  "name": "taap-jev",
  "private": true,
  "type": "module",
  "description": "TAAP booking consultant — Jev decision layer (routing + hotel). No Telegram, DB, or TAAP side effects.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "eval": "node tools/eval.mjs",
    "test": "node --test app/src/decide.test.js"
  }
}
